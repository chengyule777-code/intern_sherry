# intern_sherry
# add lf-toolbox and git.alias in my_aliases file
# ecg_removal_py Folder contains a standalone python implementation of the Adaptive Template Subtraction(ATS) algorithm. It removes ECG artifacts from EMG signals by detecting R-peaks, constructing a dynamic cardiac template, and subtracting it from the raw data to isolate clean muscle activity.
# emg_sim contains 

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "emg-sim"
version = "0.1.0"
description = "Wire-compatible EMG simulator for rr_app"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
emg-sim = "emg_sim.run:main"
